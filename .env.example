# Server credentials (WebUntis simulation)
UNTIS_USER=testuser
UNTIS_PASSWORD=testpass

# Port and school name
PORT=3000
SCHOOL_NAME=demo

# Optional: password for the /admin panel (leave empty = panel is open, no login)
# ADMIN_PASSWORD=mein-admin-passwort